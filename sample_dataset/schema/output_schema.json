{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the PDF document, extracted from the first page or metadata."
    },
    "outline": {
      "type": "array",
      "description": "A list of headings extracted from the PDF, including their level, text, and page number.",
      "items": {
        "type": "object",
        "properties": {
          "level": {
            "type": "string",
            "enum": ["H1", "H2", "H3"],
            "description": "The heading level (H1, H2, or H3)."
          },
          "text": {
            "type": "string",
            "description": "The text content of the heading."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "description": "The page number where the heading appears."
          }
        },
        "required": ["level", "text", "page"],
        "additionalProperties": false
      }
    }
  },
  "required": ["title", "outline"],
  "additionalProperties": false
}